create database miniproject;
use miniproject;

create table Student_details( name varchar(20), 
                              Rollno int(20), 
                              Class Varchar(20)
                             );
desc Student_details;
select *from Student_details;
